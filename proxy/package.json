{
  "name": "proxy",
  "private": true,
  "scripts": {
    "dev": "bun run --watch server.ts --env-file .env"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-openid-connect": "^2.20.1"
  }
}
